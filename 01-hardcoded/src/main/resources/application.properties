spring.datasource.url=jdbc:postgresql://localhost/postgres
spring.datasource.username=postgres
spring.datasource.password=v3ry_s3cr3t

castedValue=2
constructedValue=The datasource username is: ${spring.datasource.username}

# Hardcoded because it is in git and thus in the artifact (jar, war)
# Great for auditing, tracing!
# With fast CI/CD also flexible
# Not good for secrets